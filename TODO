* Fix errors shown by Closure compiler.
* Fix bug in purse implementation (on deposit we don't store new balance).
* Work on a string-to-pointer interface (and vice versa) for serialization of pointers.
* Work out standard interface e.g. "respondsTo" message.
